% ==============================================================================
% RapidBoxForest (RBF) - IEEE Transactions on Robotics submission source (EN).
% Standalone-compilable from paper/ while reusing generated assets and figures
% from paper/generated/ and paper/figures/.
% ==============================================================================
% Load the silence package as early as possible to filter benign font and box
% warnings that are emitted during package/class loading in automated builds.
\RequirePackage{silence}
\WarningFilter{latexfont}{Font shape}
\WarningFilter{latexfont}{Some font shapes were not available}
\documentclass[journal]{IEEEtran}
\hbadness=10000
\vbadness=10000

% Font and micro-typography settings for XeLaTeX: prefer a Times-like
% text font that provides small-caps shapes and reliably defined bold shapes.
% These settings reduce missing-font-shape warnings and improve paragraph
% justification behaviour under XeLaTeX.
\usepackage{fontspec}
% Use the built-in Latin Modern family directly to avoid font discovery work
% during compilation on hosted services.
\setmainfont{Latin Modern Roman}[Ligatures=TeX,Scale=MatchLowercase]
\setsansfont{Latin Modern Sans}[Scale=MatchLowercase]
\setmonofont{Latin Modern Mono}[Scale=MatchLowercase]

% If the document uses \textsc extensively but the chosen font lacks OpenType
% small-caps, avoid generating TU/.../sc requests by mapping \textsc to an
% uppercase form instead of real small-caps shapes. Use a robust definition
\DeclareRobustCommand{\textsc}[1]{\textnormal{\MakeUppercase{#1}}}

% Loosen paragraph tolerance slightly to avoid underfull hbox warnings in
% narrow IEEE two-column layout while keeping good justification.
\tolerance=2000
\emergencystretch=2em
% Keep floats close to the text that introduces them.
\setcounter{topnumber}{4}
\setcounter{bottomnumber}{3}
\setcounter{totalnumber}{6}
\renewcommand{\topfraction}{0.92}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\textfraction}{0.05}
\renewcommand{\floatpagefraction}{0.85}
\renewcommand{\dbltopfraction}{0.9}
\renewcommand{\dblfloatpagefraction}{0.85}
\setlength{\textfloatsep}{5pt plus 1pt minus 2pt}
\setlength{\dbltextfloatsep}{6pt plus 1pt minus 2pt}
\setlength{\floatsep}{4pt plus 1pt minus 2pt}
\setlength{\intextsep}{5pt plus 1pt minus 2pt}
\setlength{\abovecaptionskip}{3pt}
\setlength{\belowcaptionskip}{0pt}
\setlength{\abovedisplayskip}{5pt plus 1pt minus 2pt}
\setlength{\belowdisplayskip}{5pt plus 1pt minus 2pt}
\setlength{\abovedisplayshortskip}{4pt plus 1pt minus 2pt}
\setlength{\belowdisplayshortskip}{4pt plus 1pt minus 2pt}
\setlength{\jot}{1pt}
\makeatletter
\setlength{\@fptop}{0pt}
\setlength{\@dblfptop}{0pt}
\setlength{\@fpsep}{4pt plus 1pt minus 2pt}
\setlength{\@dblfpsep}{4pt plus 1pt minus 2pt}
\setlength{\@fpbot}{0pt plus 1fil}
\setlength{\@dblfpbot}{0pt plus 1fil}
\makeatother

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{amsthm}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{afterpage}
\usepackage{booktabs}
\usepackage{array}
\usepackage{dblfloatfix}
\usepackage{cuted}
\usepackage{rotating}
\usepackage{graphicx}
\usepackage{capt-of}
\usepackage{placeins}
\usepackage{bm}
\usepackage{xr}
\usepackage{hyperref}
\hypersetup{hidelinks}
\usepackage{cleveref}
\usepackage{etoolbox}
\usepackage[inline]{enumitem}

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem{definition}{Definition}
\newtheorem{remark}{Remark}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newtheorem{assumption}{Assumption}

\newcommand{\rbf}{\textsc{RBF}}
\newcommand{\iAABB}{\ensuremath{\mathrm{iAABB}}}
\newcommand{\Real}{\ensuremath{\mathbb{R}}}
\newcommand{\Cfree}{\ensuremath{\mathcal{C}_{\mathrm{free}}}}
\newcommand{\calO}{\ensuremath{\mathcal{O}}}
\newcommand{\Q}{\ensuremath{\mathcal{Q}}}
\newcommand{\cspace}{\mbox{C-space}}
\newcommand{\onlineq}{\mbox{Online/q}}
\newcommand{\etal}{\textit{et al.}}
\renewcommand{\algorithmicrequire}{\textbf{Input:}}
\renewcommand{\algorithmicensure}{\textbf{Output:}}
\newcommand{\algcommentline}[1]{\STATE \textit{#1}}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{M}[1]{>{\raggedright\arraybackslash}m{#1}}

\let\TableOrigTiny\tiny
\let\TableOrigScriptSize\scriptsize
\let\TableOrigFootnoteSize\footnotesize
\let\TableOrigSmall\small
\newcommand{\TableFontStepUp}{%
  \let\TableOrigTiny\tiny
  \let\TableOrigScriptSize\scriptsize
  \let\TableOrigFootnoteSize\footnotesize
  \let\TableOrigSmall\small
  \renewcommand{\tiny}{\TableOrigScriptSize}
  \renewcommand{\scriptsize}{\TableOrigFootnoteSize}
  \renewcommand{\footnotesize}{\TableOrigSmall}
  \renewcommand{\small}{\normalsize}
}
\newcommand{\TableFontRestore}{%
  \let\tiny\TableOrigTiny
  \let\scriptsize\TableOrigScriptSize
  \let\footnotesize\TableOrigFootnoteSize
  \let\small\TableOrigSmall
}
\AtBeginEnvironment{table}{\TableFontStepUp}
\AtBeginEnvironment{table*}{\TableFontStepUp}
\AtBeginEnvironment{sidewaystable}{\TableFontStepUp}

\begin{document}

\title{RapidBoxForest:\\ Link-Envelope-Guided Box Forests for Low-Overhead Scene Construction in Multi-Query Manipulation}

\author{Yu~Tian and Hongliang~Ren%
\thanks{The authors are with the Department of Electronic Engineering,
The Chinese University of Hong Kong (CUHK), Hong Kong SAR, China.
E-mail: \texttt{ty1997@link.cuhk.edu.hk}.}}

\maketitle

\begin{abstract}
Repeated-query manipulation needs reusable free-space structure, but certified
convex regions can be expensive to construct and update in cluttered workcells.
RapidBoxForest (\rbf{}) validates axis-aligned joint boxes with endpoint-to-link
swept-capsule bounds, organizes accepted boxes as a scene-level forest and typed
corridor graph, and stores kinematics-keyed envelope evidence in a Lifelong
Envelope Cache Tree (LECT) separately from scene labels. Certificates are
conditional: conservative box or corridor unions are covered, whereas repaired,
shortened, or filter-based paths require query validation.
Saved-catalog experiments use audit-separated timing and explicit cache
accounting. On Shelf+IIWA, warm depth-23 LECT replay builds the registered
structure in 0.166~s, with 0.130~s per-query online time and 0.164~s
five-query amortized time; live hierarchical interval forward kinematics
(HIFK-5) evidence materialization raises build time to 7.006~s. At registered
saved-random-scene operating points, \rbf{} has lower per-query online time
than Batch Informed Trees (BIT*) checkpoint rows in four of six
robot--difficulty panels.
In a maintenance-only study, leaf-sweep insertions are 153.3--207.5$\times$
faster and removals are 943.6--3354.0$\times$ faster than the 15-obstacle warm
rebuild. Snapshot creation, loading, resident memory, and replanning after
updates are excluded from these timing rows.
\end{abstract}

\begin{IEEEkeywords}
Motion planning, multi-query planning, link interval envelopes, box-region planning, configuration-space planning.
\end{IEEEkeywords}

\begin{table*}[!t]
\centering
\caption{Qualitative taxonomy of reusable manipulation-planning paradigms.
The comparison emphasizes the persistent object, validation granularity, and
scene-edit semantics rather than ranking planners by runtime.}
\label{tab:related_qualitative_comparison}
\fontsize{6.7}{7.2}\selectfont
\setlength{\tabcolsep}{1.6pt}
\renewcommand{\arraystretch}{1.08}
\begin{tabular}{@{}M{0.180\textwidth}M{0.125\textwidth}cM{0.155\textwidth}M{0.160\textwidth}M{0.235\textwidth}@{}}
\toprule
Family & Persistent object & Vol. & Validation and certificate unit & Scene-edit reuse & Main trade-off \\
\midrule
PRM and LazyPRM & Roadmap vertices and edges & No & Local edges or path segments &
Topology can be reused; affected edges require rechecking &
Reusable multi-query graph, but reusable evidence is zero-volume. \\
RRT-Connect and RRT & Query tree & No & Incremental path segments &
Little persistent reuse across queries &
Query-focused discovery; persistent structure is query-local. \\
BIT* and FMT* & Batch sample graph or tree & No & Candidate graph edges &
Primarily query-centric; limited scene-level reuse &
Anytime quality improvement, but still edge-centric. \\
Adaptive cell decomposition & Configuration-space cells & Yes & Cells under exact, sampled, or interval tests &
Explicit cells can be locally relabeled when maintained &
Volumetric representation; scalability depends on the cell validator. \\
IRIS and GCS & Certified convex regions and graph & Yes & Convex free-space regions &
Region reuse is scene-coupled; edits may require reinflation or recertification &
Optimization-ready regions with construction and recertification cost. \\
\rbf{} & Box forest + typed graph + LECT evidence & Yes &
Joint boxes via link envelopes; final audit when required &
Scene boxes are maintained locally; LECT replays kinematics-keyed evidence &
Lower-overhead volumetric reuse with less expressive boxes and conditional certificates. \\
\midrule
\multicolumn{6}{@{}p{0.985\textwidth}@{}}{\emph{Abbrev.:}
PRM=probabilistic roadmap; RRT=rapidly exploring random tree;
BIT*=Batch Informed Trees; FMT*=Fast Marching Tree; IRIS=Iterative Regional
Inflation; GCS=Graphs of Convex Sets; LECT=Lifelong Envelope Cache Tree;
Vol.=volumetric.} \\
\bottomrule
\end{tabular}
\end{table*}

% ==============================================================================
\section{Introduction}\label{sec:intro}

Many high-degree-of-freedom manipulation workloads contain related queries in
nearly fixed workcells. Robot kinematics persist, while start-goal pairs,
local obstacles, or small scene edits change. Convex-region planners,
including IRIS (Iterative Regional Inflation) and Graphs of Convex Sets (GCS),
provide optimization-ready region-level abstractions once regions are
certified~\cite{deits2015computing,marcucci2023motion,marcucci2024shortest}, but region
construction can dominate in high-dimensional or frequently edited scenes.
Sampling-based planners, including PRM and RRT variants, avoid such region
construction~\cite{lavalle2006planning,lavalle1998rapidly,
kavraki1996probabilistic,karaman2011sampling}, but their reusable structures
are usually zero-volume roadmaps or query trees.

\rbf{} targets this gap with a lower-overhead volumetric model: it grows
axis-aligned joint-space boxes, validates them with link-envelope bounds,
connects accepted boxes into a typed corridor graph, and reuses that graph
across later queries. The resulting trade-off is explicit: boxes are less
expressive than rich convex covers, but they simplify validation, graph
building, updates, and multi-query reuse.

\paragraph{Problem statement}
Let $\Q\subset\Real^n$ be the joint-limit box of a fixed manipulator and let
$\calO\subset\Real^3$ be the current workspace obstacle set. Given a set or
stream of queries $(q_s,q_g)\in\Q^2$, \rbf{} builds a finite family of
validated \cspace{} boxes and an adjacency graph over those boxes. The design
goal is low scene-construction and maintenance overhead while retaining
reusable volumetric free-space evidence.

The geometric core adapts classical endpoint bounding. For rounded links,
endpoint envelopes bound endpoint trajectories over a joint box; their
radius-expanded convex hull encloses the swept link and validates \cspace{}
boxes. Conservative sources yield conditional box certificates, whereas tighter
non-certifying sources remain filters whose paths require query validation.
Reuse is split accordingly: scene-level \rbf{} caches store one scene's box
forest and corridor graph, while the Lifelong Envelope Cache Tree (LECT) stores
replayable kinematics-keyed endpoint and link evidence.

The evaluation preserves this boundary. Success is counted only after final
query validation unless a row is explicitly conservative. Warm Shelf timings
include replay-based scene construction and per-query retrieval; snapshot
creation, loading, resident memory, and offline setup amortization are excluded.
Dynamic update rows measure scene-cache maintenance, not end-to-end query
pipelines. Thus the experiments evaluate low-overhead reusable-region
construction under a stated validation policy, not dominance over single-query
planners, repair-free certification, or completeness. This work contributes:
\begin{enumerate}[nosep,leftmargin=*]
  \item \emph{Box validation:} conservative endpoint-to-link envelopes validate
  boxes; staged sources remain query-validated filters.
  \item \emph{Planning object:} validated boxes form a reusable scene forest and
  typed corridor graph.
  \item \emph{Two-level reuse:} scene caches hold boxes and graph state; LECT
  replays kinematics-keyed envelope evidence.
  \item \emph{Local maintenance:} deletions promote blocked domains; insertions
  trigger bounded refill.
  \item \emph{Evaluation protocol:} saved catalogs, audit-separated timing, and
  cache accounting define the reported scope.
\end{enumerate}

Sections~\ref{sec:link_envelopes}--\ref{sec:exp} cover link envelopes, LECT,
forest construction and updates, and evaluation.

\begin{figure*}[!t]
\centering
\includegraphics[width=0.97\textwidth]{figures/framework.png}
\caption{\rbf{} pipeline for link-envelope-guided multi-query planning. Top:
endpoint interval envelopes induce link interval envelopes over a
configuration-space (\cspace{})
box. Middle: LECT reuses kinematics-keyed endpoint and link-envelope records
across repeated builds. Bottom: scene filtering and sample-guided growth expand
a connected scene-level box forest for repeated corridor queries.}
\label{fig:linkiaabb}
\end{figure*}

% ==============================================================================
\section{Related Work}\label{sec:related}

Convex-region planners construct certified free-space covers and search or
optimize on the induced graph. IRIS variants inflate collision-free convex
regions, and Graphs of Convex Sets (GCS) make the
resulting graph optimization explicit~\cite{deits2015computing,marcucci2023motion,
marcucci2024shortest,amice2022finding,petersen2023growing,werner2024approximating}. \rbf{} shares
the region-level objective but uses axis-aligned \cspace{} boxes; this restricted
geometry limits expressivity while keeping validation, adjacency, and update
operations local and explicit.
\Cref{tab:related_qualitative_comparison} summarizes the qualitative position.

\paragraph{Continuous-collision-detection endpoint bounds}
\label{sec:related_novelty}
Swept-volume approximation and interval forward kinematics supply the geometric
primitives used here. Convex-hull generator bounds, capsule radius lifts, the Proximity
Query Package (PQP), Gilbert--Johnson--Keerthi (GJK), the Flexible Collision
Library (FCL), and interval Denavit--Hartenberg propagation all support swept-sphere or
endpoint-bounded collision reasoning~\cite{blackmore1992swept,abdel2006swept,
gottschalk1996obbtree,larsen1999fast,gilbert1988fast,bergen1999gjk,
pan2012fcl,moore2009introduction,jaulin2001applied,snyder1992interval,
merlet2004solving,merlet2009interval}. Redon \etal{} apply this pipeline to
articulated-body continuous collision detection over time
steps~\cite{redon2004ccd_avatar,redon2005fast_artic}; related Taylor-model and
safety methods use similar swept-sphere
bounds~\cite{zhang2007taylor,schwarzer2005adaptive,corrales2011ssl}. \rbf{} uses
joint-box intervals rather than trajectory time steps as the bounding domain,
stores compatible interval evidence in LECT, and assembles accepted boxes into
a typed corridor graph. Repaired or post-processed paths remain subject to
query validation.

\paragraph{Reuse and adaptive decomposition}
Multi-query planners amortize effort through reusable graphs. Probabilistic
roadmaps (PRMs) and LazyPRM reuse roadmaps and edge checks~\cite{kavraki1996probabilistic,bohlin2000path,
dellin2016unifying,schmerling2015optimal}, experience-based systems reuse prior
paths~\cite{coleman2014moveit}, and adaptive cell methods split cells until
the accepted-cell graph can be searched~\cite{lozanoperez1983spatial,
brooks1985subdivision}. \rbf{} combines these ideas with box-level envelope
validation and a separate kinematics-keyed evidence cache.

\paragraph{Sampling baselines and refinement}
RRT*, RRT-Connect, Batch Informed Trees (BIT*), Fast Marching
Tree (FMT*), and the Open Motion Planning Library (OMPL) provide standard
single-query or anytime implementations and benchmarking
infrastructure~\cite{karaman2011sampling,kuffner2000rrt,gammell2015batch,
gammell2018informed,janson2015fmt,sucan2012ompl,moll2015benchmarking}.
Trajectory optimizers refine supplied
paths~\cite{ratliff2009chomp,kalakrishnan2011stomp,schulman2014motion,
mukadam2018gpmp2}. \rbf{} is complementary: it supplies reusable volumetric
context before repair, smoothing, and final validation.



% ==============================================================================
\section{Link Interval Envelopes}\label{sec:link_envelopes}

The link-envelope validation layer maps joint regions to endpoint and link
bounds. For a joint region
$I\subseteq\Q$, endpoint envelopes $E_k(I)$ bound endpoint trajectories and
link envelopes $B_\ell(I)$ bound rounded links over the same region:
\[
I \mapsto \{E_k(I)\} \mapsto \{B_\ell(I)\}.
\]
Axis-aligned joint boxes are the default regions; local oriented bounding boxes
(OBBs) in scaled joint coordinates are used only for typed bridge corridors.
Conservative endpoint evidence gives certificates. Empirical or tighter
performance-oriented endpoint sources are filters and must be followed by query
validation.

\begin{assumption}[Collision model for certificates]
\label{ass:collision_model}
Certificates cover configured active rounded links against $\mathcal{O}$, with
joint limits enforced by $I\subseteq\Q$. Other contacts or constraints
(self-collision, attachments, grippers, task regions) are outside this
certificate unless encoded in the validation policy and cache descriptor;
experiments check only active-link--workspace obstacles.
\end{assumption}

\subsection{Geometric Foundation}\label{sec:rewrite_geom}

Let $T(q)x=R(q)x+t(q)$ be the transform at configuration $q$, and let
$\mathcal{S}_I(C_0)$ denote the workspace set swept by body $C_0$ as
$q$ ranges over $I$. For a capsule $C=S\oplus B_2(r)$, the fixed radius ball
commutes with rigid motion:
\[
\mathcal{S}_I(C)=\mathcal{S}_I(S)\oplus B_2(r).
\]
Thus a rounded link can be bounded by first bounding the zero-radius center
segment and then lifting by the link radius. For a convex body
$P=\operatorname{conv}(v_1,\ldots,v_m)$, outer bounds on the generator
trajectories also bound the swept body,
\[
\mathcal{S}_I(P)\subseteq
\operatorname{conv}\Bigl(\bigcup_i \widehat{\Gamma}_i(I)\Bigr).
\]
For a capsule link, only the two endpoint trajectories are needed. Let
\(\Gamma_a(I)\) and \(\Gamma_b(I)\) denote the exact workspace trajectories of
the proximal and distal endpoints over \(I\). \rbf{} uses these classical
swept-sphere facts over a joint-box domain and stores the result as reusable
planning evidence.

\begin{proposition}[Endpoint envelopes induce link interval envelopes]
\label{prop:endpoint_to_link_envelope}
Consider a rounded link $L=[a,b]\oplus B_2(r)$ and a joint interval $I$. If
endpoint envelopes $E_a(I)$ and $E_b(I)$ satisfy
$\Gamma_a(I)\subseteq E_a(I)$ and $\Gamma_b(I)\subseteq E_b(I)$, then
\[
\mathcal{S}_I(L)
\subseteq
\operatorname{conv}\bigl(E_a(I)\cup E_b(I)\bigr)\oplus B_2(r).
\]
Consequently, any outer representation of the right-hand side is a valid link
interval envelope for the whole joint box $I$.
\end{proposition}

\begin{proof}
For each $q\in I$, the center segment of the moved link is the convex hull of
the two moved endpoints, so
$T(q)[a,b]\subseteq\operatorname{conv}(\Gamma_a(I)\cup\Gamma_b(I))$ over the
entire interval. The endpoint inclusions place this set inside
$\operatorname{conv}(E_a(I)\cup E_b(I))$. Adding the fixed radius ball commutes
with the rigid-motion sweep of the capsule, giving the stated containment.
\end{proof}

\begin{corollary}[Conservative joint-box validation]
\label{cor:box_validation}
Let $I\subseteq\Q$ be a joint box and let $\{B_\ell(I)\}$ be conservative link
interval envelopes for all active rounded links. If
$B_\ell(I)\cap\calO=\emptyset$ for every link $\ell$, then every configuration
$q\in I$ is collision-free with respect to the collision model in
\Cref{ass:collision_model}.
\end{corollary}

\begin{proof}
By \Cref{prop:endpoint_to_link_envelope}, each conservative link envelope
contains the swept workspace volume of its link over all configurations in
$I$. Disjointness from $\calO$ for every active link therefore excludes
workspace obstacle collision for the whole manipulator at every $q\in I$ under
\Cref{ass:collision_model}.
\end{proof}

\paragraph{Numerical conservatism}
A certificate is attached only when endpoint propagation, radius padding, and
obstacle-disjointness tests are outward bounds under
\Cref{ass:collision_model}. Axis-aligned bounding box (AABB) padding uses a
conservative $B_\infty$ radius lift. SupportHull/GJK is certificate-backed only
with outward obstacle inflation, margins, and termination tolerances. Empirical
endpoint sources or post-processing outside the conservative box or
OBB-corridor union are filters and do not extend \Cref{cor:box_validation} or
\Cref{thm:corridor_certificate}.

\subsection{Endpoint Interval Envelopes}\label{sec:rewrite_endpoint_env}

For endpoint $k$, let $\Gamma_k(I)$ be its exact trajectory over interval $I$.
Any conservative set containing $\Gamma_k(I)$ is an endpoint interval envelope;
the basic record is an endpoint AABB because it is compact and directly reusable
by all link-envelope representations. The planner uses two certificate-backed
sources. Interval forward kinematics (IFK) propagates affine-arithmetic bounds
along the Denavit--Hartenberg chain; shared noise symbols preserve some
trigonometric correlation, making it a low-latency certificate-backed endpoint
source in this implementation.
Hierarchical IFK (HIFK) bisects the joint interval and hulls child endpoint
boxes, reducing wrapping at higher materialization cost. IFK is the unsplit
affine-arithmetic pass; HIFK is a true hierarchical split-and-hull evaluation
(for example, HIFK-5 means five split levels), so the split depth is part of the
cache descriptor and cannot be exchanged with an unsplit record. A
critical-sample source probes interval endpoints,
midpoints, near-boundary configurations, and trigonometric turning candidates;
it can tighten boxes that IFK wraps, but does not certify coupled extrema.
An analytical extrema source solves low-dimensional sinusoidal boundary
subproblems, reducing one- and two-joint boundary cases to structured root
search, and uses interval closure only when a conservative bound is needed.
These two tighter sources serve as diagnostics or filters:
any path depending on them is accepted only after final query validation unless
wrapped by an independent conservative certificate. During tree descent,
transient prefix transforms are reused to avoid recomputing unaffected chain
prefixes; this is a runtime materialization optimization rather than persistent
scene state. Fig.~\ref{fig:linkiaabb} shows the endpoint-to-link pipeline, LECT
reuse, and box-forest use.

\subsection{C-space OBB Endpoint Envelopes}\label{sec:obb_endpoint_env}

Axis-aligned boxes remain the global forest primitive. Bridge repair motions
can be joint-correlated, so \rbf{} may validate a
local typed \cspace{} OBB corridor without replacing the dyadic forest. OBBs are
defined in scaled joint coordinates.
Let \(y=S(q-q_{\rm ref})\), where \(S\) is a diagonal joint scaling, and let
\[
\begin{aligned}
R_{\rm obb}
&=
\{\,q_c + A\xi \mid \xi\in\Xi\,\},\\
\Xi
&=\prod_{i=1}^{m}[-1,1],
\qquad
A = S^{-1}U\operatorname{diag}(h),
\end{aligned}
\]
with \(U\) orthonormal in the scaled metric and \(h\) the halfwidth vector. The
object \(R_{\rm obb}\) is a configuration-space region, not a workspace OBB:
the certificate comes from endpoint and link envelopes over \(R_{\rm obb}\), not
from the fitted orientation.

\paragraph{Cover certificate}
A conservative proof tiles the local cube, maps each tile to joint space, and
validates the resulting AABB hulls:
\[
I_r=\operatorname{bbox}_q(q_c+A X_r).
\]
If every \(I_r\) passes the standard conservative link-envelope scene test, then
\[
R_{\rm obb}\subseteq \bigcup_r I_r \subseteq \Cfree,
\]
where \(\Cfree\) is the collision-free configuration set under
\Cref{ass:collision_model}. The stored OBB certificate is then a finite cover
of ordinary box certificates.

\paragraph{Correlated endpoint envelope}
A tighter source keeps the local OBB variables \(\xi\) correlated through the
forward-kinematics computation:
\[
q_j=q_{c,j}+a_j^\top\xi,\qquad \xi_i\in[-1,1],
\]
where \(a_j^\top\) is the \(j\)-th row of \(A\). Affine-arithmetic propagation
then produces endpoint records
\[
E_k(R_{\rm obb})
=
c_k+\sum_i g_{k,i}\epsilon_i \oplus [-\rho_k,\rho_k].
\]
Equivalently,
\[
E_k(R_{\rm obb})
=
p_k(q_c)+J_k(q_c)A\Xi \oplus [-\rho_k,\rho_k],
\]
where each coordinate remainder is bounded conservatively, for example by
\[
\rho_{k,\mu}\ge
\frac{1}{2}\sum_{a,b}
\left|{\left(A^\top H_{k,\mu}A\right)}_{ab}\right|,
\]
with \(H_{k,\mu}\) an interval or otherwise conservative Hessian bound over
\(R_{\rm obb}\). If these remainders are sampled rather than outward bounded,
the OBB source is a performance filter and not a certificate.

OBB endpoint records feed the same endpoint-to-link construction as joint boxes.
For a zonotope-plus-box endpoint record,
\[
h_{E_k}(d)=d^\top c_k+\sum_i |d^\top g_{k,i}|
          +\sum_{\mu=1}^3 |d_\mu|\rho_{k,\mu}.
\]
The zero-radius link centerline support is
\[
h_\ell^\circ(d)=\max\{h_{E_a}(d),h_{E_b}(d)\},
\]
and the rounded-link envelope adds the capsule radius,
\[
h_\ell(d)=h_\ell^\circ(d)+r_\ell\|d\|_2.
\]
Coordinate-axis queries of \(h_\ell\) provide an AABB broadphase, while the same
support record can be used by the SupportHull/GJK narrow phase. A
conservative OBB corridor edge therefore becomes another
conservative region in the typed query graph, separate from the persistent
dyadic LECT tree unless promoted to a scene-local graph layer.

\subsection{Link-Envelope Representations}\label{sec:rewrite_link_env}

Endpoint records can be exported to multiple link representations without
changing the upstream kinematic source. For a capsule link,
$B_\ell(I)=B_\ell^\circ(I)\oplus\mathcal{R}(r_\ell)$, where
$B_\ell^\circ(I)$ bounds the centerline sweep and $\mathcal{R}$ is the chosen
radius lift.

\paragraph{AABB envelope}
AABB envelopes wrap the two endpoint envelopes in one box and pad by the link
radius:
\[
B_\ell(I) = \operatorname{bbox}\bigl(E_{\ell-1}(I) \cup E_\ell(I)\bigr)
\oplus B_\infty(r_\ell).
\]
Segment subdivision can retain a union of padded sub-envelopes for tighter
filtering, but the union must remain explicit to preserve the gain.

\paragraph{Fixed-direction slab envelope}
A \(k\)-discrete oriented polytope (\(k\)-DOP) record stores lower and upper
support values along a fixed direction set. Endpoint boxes are projected onto
those directions, padded by the link radius, and merged over retained segment
pieces. In the runtime pipeline, these slab tests form an inexpensive
intermediate filter: after the ordinary AABB rejection, non-axis directions can
reject near misses before invoking GJK. They are filters unless
their endpoint source and separation tests are conservative.

\paragraph{SupportHull envelope}
SupportHull keeps the two endpoint AABBs and link radius as a compact support
record. For direction $d$, the support point is selected from the proximal or
distal endpoint box, and obstacle refinement uses a fixed-size GJK narrow phase
with outward obstacle inflation. This preserves more directional structure than
a single AABB while keeping the cached record compact. When present,
fixed-direction slab records act only as an intermediate filter between the AABB
broadphase and SupportHull/GJK refinement; they do not change the certificate
condition, which still depends on conservative endpoint evidence and outward
scene tests.

% ==============================================================================
\section{Lifelong Envelope Cache Tree}\label{sec:lect}

LECT is a replay layer below the scene-level \rbf{} cache. It stores
scene-independent endpoint and link-envelope evidence keyed by robot kinematics
and joint intervals. Obstacle tests, box labels, graph connectivity, and query
outcomes remain scene-local; every replayed envelope must still be checked
against the current obstacle set.

\subsection{Cache Key and Split Policy}\label{sec:lect_design}

LECT is a dynamically grown binary k-dimensional (KD) tree over $\Q$. A node
stores its joint interval, split metadata, cached endpoint evidence, and runtime
residency state. The central invariant is that evidence is \emph{kinematics-keyed}
rather than \emph{scene-keyed}: replay imports geometry bounds, never a
free-space label.

\paragraph{Evidence/label separation}
An envelope record is replay-compatible only when the robot model, endpoint
source, envelope representation, split descriptor, link radii, and active-link
set match the current build. The split policy is also part of this descriptor;
alternative policies are separate cache settings. For a parent interval $I$, let
$I_d^{-}$ and $I_d^{+}$ be the two children
obtained by splitting joint dimension $d$ at its midpoint. Define the cumulative
downstream envelope volume as
\[
\mathcal{V}(I) := \sum_{\ell} \operatorname{vol}(B_\ell(I)).
\]
One implemented schedule chooses depth-wise split dimensions that minimize the
worst-child cumulative envelope volume,
\[
d^* = \arg\min_d
\max\!\left\{\mathcal{V}(I_d^-),\mathcal{V}(I_d^+)\right\}.
\]
Depth-synchronous schedules keep replay deterministic; if the scheduled
dimension is unavailable at a node, the runtime falls back to the widest valid
dimension. Candidate dimensions are filtered by active-link reachability:
splitting a joint that cannot affect any retained endpoint cannot tighten the
downstream envelopes and is skipped for that descriptor.

\paragraph{Sparse interval staging}
Deep cells need not materialize every ancestor. A planning cell can carry its
exact joint interval, split counts, and descriptor fingerprint; if an exact
replay record is absent, the interval is materialized on demand and then validated
against the scene. Scene labels remain only in the forest or partition layer.

\subsection{Runtime Use and Validation Boundary}

LECT serves local box materialization; graph search uses the scene forest. Given
a joint interval, the runtime replays or materializes endpoint evidence, derives
the requested link representation, and then runs the current scene-validation
policy. AABB
broadphase normally precedes SupportHull/GJK refinement. Parent or child
records can accelerate materialization, but no replayed record alone is a
standalone free-space certificate. When a node is split, child envelopes may
tighten the parent witness, but the resulting parent record is still
representation-dependent and must be checked again against \(\calO\).
Directional records are staged filters: a box first survives the padded
AABB test, and only near survivors reach slab or SupportHull/GJK refinement. If
no exact replay record exists, the interval is materialized on demand and then
handled by the same validation path, so a cache miss changes cost but not
semantics.

\subsection{Storage and Warm-Build Semantics}\label{sec:lect_storage}

Persistence is selective. Endpoint records and derived link-envelope records are
the reusable evidence units. IFK+AABB records can be regenerated on demand,
whereas slab or SupportHull records are stored only when their tighter filters
justify lookup and storage cost. Prewarm fills target leaf records and
reconstructs parents by child-hull propagation, avoiding repeated direct
forward-kinematics charges at multiple depths for the same cache. In a warm
build, a cache hit reconstructs the requested link envelopes and revalidates
them against the current obstacles. Warm builds still regrow scene-specific
boxes; persisted box labels are not imported as certificates. The registered
Shelf replay is read-only during timing, so the replay store does not accumulate
new evidence during those measurements. Random-scene cache growth is treated
separately as part of the per-robot profile.

\begin{figure*}[!t]
\centering
\begin{tabular}{@{}c@{\hspace{2pt}}c@{\hspace{2pt}}c@{}}
\includegraphics[width=0.292\textwidth]{figures/sbf_grow_1.png} &
\includegraphics[width=0.292\textwidth]{figures/sbf_grow_3.png} &
\includegraphics[width=0.292\textwidth]{figures/sbf_grow_5.png} \\
{\footnotesize (a) C-space cells \& box $Q$} &
{\footnotesize (b) C-space path} &
{\footnotesize (c) Invalidated boxes} \\[2pt]
\includegraphics[width=0.292\textwidth]{figures/sbf_grow_2.png} &
\includegraphics[width=0.292\textwidth]{figures/sbf_grow_4.png} &
\includegraphics[width=0.292\textwidth]{figures/sbf_grow_6.png} \\
{\footnotesize (d) Workspace AABBs of $Q$} &
{\footnotesize (e) Workspace path} &
{\footnotesize (f) After regrowth} \\
\end{tabular}
\caption{Two-link schematic of the \rbf{} build, query, and
obstacle-update cycle. A highlighted \cspace{} box maps to workspace AABBs; the
box corridor yields a path; obstacle insertion invalidates intersecting boxes
and triggers local refill without a full rebuild. The panels illustrate the
build, query, and update logic rather than the robot geometry, workspace scale,
or dimensionality of the reported experiments.}
\label{fig:overview}
\end{figure*}

% ==============================================================================
\section{RapidBoxForest Construction}\label{sec:forest}

Fig.~\ref{fig:overview} illustrates the planning layer. \rbf{} builds a
scene-specific set of validated joint boxes and stores their adjacency as a
typed corridor graph. This pair is the scene cache: while the obstacle set is
unchanged, later queries use box membership and graph search instead of
rebuilding the cover. LECT supplies reusable kinematics-keyed evidence, but every
accepted scene box is still checked against the current obstacles.

A box is an axis-aligned joint interval
$B=[l^{(1)},u^{(1)}]\times\cdots\times[l^{(n)},u^{(n)}]\subseteq\Q$.
A \emph{validated box} is admitted by
the configured scene-validation policy. When that policy is the conservative
link-envelope disjointness test, the box is a free-space certificate; when a
tighter performance filter is used, the box is only a planning region and any
candidate path must pass query validation. Cells that do not pass validation
are not inserted into the forest; selected blocked or inconclusive domains are
retained for local refinement or later update.

\subsection{Construction Objective}\label{sec:forest_objective}

Given the joint-limit box $\Q$ and obstacle set $\calO$, the build stage
constructs
\[
\mathcal{F}=\{B_1,\dots,B_N\}, \qquad B_i\subseteq\Q,
\]
where each $B_i$ satisfies the scene-validation policy. This budgeted set is
concentrated around task anchors, query-relevant frontiers, and connectors. The
reusable output is the pair $(\mathcal{F},G)$, where $G$ records adjacency and
auxiliary transitions.

For boxes
\[
B_i=\prod_{d=1}^{n}[l_i^{(d)},u_i^{(d)}],\qquad
B_j=\prod_{d=1}^{n}[l_j^{(d)},u_j^{(d)}],
\]
\rbf{} treats them as connected when every coordinate overlaps or is separated
by at most a small tolerance $\varepsilon_c$:
\[
\max\{l_i^{(d)},l_j^{(d)}\}
\le
\min\{u_i^{(d)},u_j^{(d)}\}+\varepsilon_c,\qquad d=1,\ldots,n.
\]
Only zero-gap overlap or touching directly yields a contained box corridor.
Tolerance-gap and explicit segment edges are search aids that require query
validation unless they are converted into conservative box or OBB corridors.

\subsection{Build Pipeline}

The build proceeds through four stages. First, local materialization descends
LECT around a seed and returns the first interval on that descent accepted by
the scene policy. Second, a leaf sweep classifies shallow cells and records
blocked domains for possible refinement or later edits. Third, frontier growth
adds boxes near anchors and query-relevant boundaries. Finally, consolidation
merges or removes redundant boxes only when the resulting boxes still satisfy
the same validation rule. \Cref{alg:ffb_rewrite,alg:leaf_sweep_refine} define
the two validation routines reused throughout this pipeline; frontier growth
and consolidation repeat the same materialize--validate--connect pattern under
budget limits and do not introduce a separate validation primitive.

\paragraph{Implementation controls}
Validation semantics and timings are governed by the build budget, LECT descriptor,
endpoint and link representation, admitted edge types, and final audit resolution.
Materialization failures remain deferred domains for restricted refinement;
frontier or repair witnesses count as certified only after conversion into
conservative box or OBB corridors. Lower-level scheduling
constants are fixed in the artifact manifest.

\begin{algorithm}[t]
\caption{Local box materialization around a seed.}
\label{alg:ffb_rewrite}
\footnotesize
\begin{algorithmic}[1]
\REQUIRE Seed $q$, LECT $T$, obstacle set $\calO$, depth cap $D_{\max}$
\ENSURE Validated interval or failure
\STATE start from the root interval containing $q$
\WHILE{depth $\le D_{\max}$}
  \STATE replay or materialize endpoint and link evidence for the current node
  \IF{the current interval passes the scene-validation policy}
    \RETURN current interval
  \ENDIF
  \STATE split and descend to the child containing $q$
\ENDWHILE
\RETURN failure
\end{algorithmic}
\end{algorithm}

\begin{algorithm}[t]
\caption{Adaptive leaf sweep and local refinement.}
\label{alg:leaf_sweep_refine}
\footnotesize
\begin{algorithmic}[1]
\REQUIRE LECT $T$, obstacle set $\calO$, sweep budget
\ENSURE Initial forest $\mathcal{F}$ and retained collision domains $\mathcal{C}$
\STATE classify priority cells by replaying or materializing LECT evidence
\STATE insert cells that pass validation into $\mathcal{F}$; prune covered descendants
\STATE retain inconclusive or blocked cells in $\mathcal{C}$
\STATE refine only selected domains in $\mathcal{C}$ using local materialization
\STATE insert refined boxes that remain inside their domain and connect to the forest
\RETURN $\mathcal{F},\mathcal{C}$
\end{algorithmic}
\end{algorithm}

\subsection{Corridor Query and Certificate}
\label{sec:forest_graph}

After consolidation, the forest is exported as a graph
$G=(V,E_{\cap}\cup E_{\varepsilon}\cup E_s\cup E_o\cup E_{\pi})$ with one vertex
per validated box. $E_{\cap}$ denotes overlap or contact edges; these are the
edges that can support a contained conservative box corridor. $E_{\varepsilon}$
denotes tolerance-gap candidates, $E_s$ explicit segment witnesses, $E_o$
conservative OBB transition corridors, and $E_{\pi}$ compressed portal chains
of conservative internal boxes. In reporting, $E_{\varepsilon}$, $E_s$,
performance-mode boxes, and post-processing outside the conservative box or
OBB-corridor union are accepted only after query validation.

Given start and goal configurations, the query stage locates their containing
or anchor boxes and searches $G$ for a typed edge sequence. If the recovered
geometric path stays inside conservative boxes, conservative OBB corridors, or
expanded portal chains of conservative internal boxes, it inherits the
certificate below. Otherwise it is a
validated planning result, not a conservative box-corridor proof.

\begin{samepage}
\begin{theorem}[Conditional conservative box-corridor path]
\label{thm:corridor_certificate}
Let $\mathcal{F}$ be a forest whose boxes have each passed conservative
link-envelope validation against obstacle set $\calO$. If a path
$\gamma:[0,1]\to\Q$ is continuous and satisfies
\[
\gamma([0,1])\subseteq \bigcup_{B\in\mathcal{F}}B,
\]
then every configuration on $\gamma$ is collision-free with respect to the
collision model in \Cref{ass:collision_model}.
\end{theorem}
\end{samepage}

\begin{proof}
For any $s\in[0,1]$, the containment assumption gives a conservatively
validated box $B\in\mathcal{F}$ with $\gamma(s)\in B$. By construction, validation of $B$
means every conservative link envelope for $B$ is disjoint from $\calO$.
Because each such envelope contains the link geometry for every configuration
inside $B$ (\Cref{cor:box_validation}), the robot geometry at $\gamma(s)$ is
also disjoint from $\calO$. Since $s$ was arbitrary, the whole path is
collision-free.
\end{proof}

\noindent\emph{Scope.}
The theorem establishes collision-freedom only for paths contained in
conservatively validated regions. It does not establish coverage or
completeness: finite-budget coverage depends on anchors, depth, refinement
budget, envelope tightness, and obstacle geometry. Paths using uncovered space,
residual segments, smoothing, or performance-mode boxes are reported only after
independent query validation.

\subsection{Dynamic Obstacle Updates}
\label{sec:dynamic_update_method}

For obstacle edits, robot kinematics and LECT records stay fixed; only scene
labels and graph edges change. Deletions may promote cached blocked domains
after edited-scene validation. Insertions invalidate conflicting boxes and
witnesses, then run bounded local refill. The update is local by design: it
maintains the represented forest and does not claim to rediscover all newly
free regions after an edit.

\begin{algorithm}[t]
\caption{Obstacle deletion promotion and insertion refill.}
\label{alg:dynamic_update}
\footnotesize
\begin{algorithmic}[1]
\REQUIRE Forest $\mathcal{F}$, typed graph $G$, collision-domain cache $\mathcal{C}$, edit $(\Delta^{-},\Delta^{+})$
\ENSURE Updated forest, typed graph, and collision-domain cache
\IF{$\Delta^{-}\neq\emptyset$}
  \STATE recheck cached blocked domains affected by deleted obstacles
  \STATE promote only domains that pass edited-scene validation and adjacency checks
\ENDIF
\IF{$\Delta^{+}\neq\emptyset$}
  \STATE remove boxes and typed witnesses hit by inserted obstacles
  \STATE run a bounded local leaf sweep inside each invalidated box
  \STATE insert replacement boxes that validate and connect to the surviving forest
\ENDIF
\STATE revalidate affected typed edges and update components
\RETURN updated $(\mathcal{F},G,\mathcal{C})$
\end{algorithmic}
\end{algorithm}

After an update, retained or inserted boxes must satisfy the edited-scene
validation policy, and typed edges must keep their original semantics. If the
updated graph cannot provide a contained conservative corridor, the candidate
path is accepted only after independent query validation.

\section{Experiments}\label{sec:exp}

The experiments evaluate the reported \rbf{} implementation under saved scenes,
fixed validation policy, and audit-separated timing. The Shelf+IIWA profile
study uses the KUKA LBR iiwa shelf-workcell benchmark and selects the
registered \rbf{} configuration. Cross-algorithm and random-scene rows use the
same success definition, audit step, final-simplification budget, and
path-length statistic unless a plotted checkpoint curve explicitly sweeps a
planner budget. Dynamic-update rows report only leaf-sweep scene-cache
maintenance. Random scenes and queries are saved before planner execution.

\begin{table*}[!t]
  \centering
  \begin{minipage}{0.74\textwidth}
  \input{generated/tab_tro_shelf_ablation.tex}
  \end{minipage}
\end{table*}

\paragraph{Protocol}
Timings are wall-clock measurements on an Ubuntu 22.04 workstation with an
Intel i5-12600KF CPU and 31~GiB memory. The generated
tables and figures are tied to provenance manifests that record sources, thread
and simplification policies, checksums, and artifact-integrity checks. The
manifests record an 8-thread budget for \rbf{} and LECT work and matching
math-library thread limits; OMPL RRT-Connect, PRM, and BIT* are
algorithmically single-threaded in these runners unless planner-internal
parallelism is exposed. PRM and
IRIS-NP/GCS (nonlinear-programming IRIS followed by GCS optimization) report
reusable builds, whereas RRT-Connect and BIT* are single-query rows. These rows
provide timing context under the shared audit protocol, but their reusable-state
semantics are not identical.
Success requires query validation with the fixed 0.01-rad segment step. \onlineq{}
denotes per-query online time and excludes final simplification and the
0.01-rad audit. Where per-query audit time (Audit/q) is present,
audit-complete time is \onlineq{} plus the
fixed 0.010~s final-simplification budget and Audit/q. Numeric entries are median
\([Q_1,Q_3]\), where \(Q_1\) and \(Q_3\) denote first and third quartiles.
\(L/L^\star\) uses success-only audited path references at the query level
unless a caption states otherwise. The main
tables are final-validation summaries, not conservative-corridor certificates,
repair-free results, or parameter-sensitivity studies. Unless stated otherwise,
runs check only active rounded links against \(\calO\); self-collision, attached
objects, grippers, and task-specific forbidden regions require additional scene
checks before using the certificate.

\paragraph{Cache and profile boundary}
Warm-cache rows include compatible LECT lookup and evidence reads. Snapshot
creation, loading, resident memory, and offline setup amortization are outside
those rows. The label d23 denotes the registered depth-23 LECT snapshot:
16,777,215~records, 12.75\,GiB on disk, and 958.079~s \mbox{prewarm} time.
Unless stated otherwise, the entries in Table~\ref{tab:tro-shelf-ablation}
replay compatible d23 evidence; the Live HIFK-5 row instead materializes HIFK-5
evidence live. The label b100 denotes the registered 100-box Shelf operating
point. The baseline row uses the SupportHull representation with the standard
AABB broadphase. Cache reuse requires matching robot model,
endpoint source, link representation, split/depth descriptor, radii, and
active-link set. Timing remains audit-separated, and bridge or repair
witnesses count as certified only after conversion into conservative box or OBB
corridor edges.

\begin{figure}[!t]
  \centering
  \includegraphics[width=\columnwidth]{figures/Marcucci_scene.png}
  \caption{Canonical Marcucci Shelf+IIWA scene with the fixed saved
  five-anchor query cycle.}
  \label{fig:tro_shelf_scene}
\end{figure}

\begin{figure*}[!t]
  \centering
  \begin{minipage}[t]{0.32\textwidth}
    \centering
    \includegraphics[width=\linewidth,keepaspectratio]{generated/fig_tro_shelf_tradeoff.pdf}
    \caption{Shelf+IIWA \rbf{} mechanism trade-off. Mechanism rows that solve
    all saved queries are plotted at Build/5 + \onlineq{} versus \(L/L^\star_{\mathrm{q}}\);
    gold rings mark the rows reported in Table~\ref{tab:tro-shelf-ablation}.}
    \label{fig:tro_shelf_tradeoff}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.64\textwidth}
    \centering
    \includegraphics[width=\linewidth,keepaspectratio]{generated/fig_tro_shelf_cross_tradeoff.pdf}
    \caption{Shelf+IIWA cross-algorithm trade-off. Black rings mark the first
    points that solve all saved queries. Timings use the \onlineq{} column of
    Table~\ref{tab:tro-shelf-cross-algorithm}; reusable-build terms are added
    only for methods that report a reusable build.}
    \label{fig:tro_shelf_cross_tradeoff}
  \end{minipage}
\end{figure*}

\begin{table*}[!t]
  \centering
  \TableFontRestore
  \input{generated/tab_tro_shelf_cross_algorithm.tex}
\end{table*}

\begin{table*}[!t]
  \centering
  \TableFontRestore
  \input{generated/tab_tro_random_summary.tex}
  \vspace{-0.50ex}
  \includegraphics[width=0.88\textwidth,keepaspectratio]{generated/fig_tro_random_tradeoff.pdf}
  \captionof{figure}{Saved-catalog random-scene trade-off. Left and middle:
  medium and hard Build/5 + \onlineq{} with query-level path
  references; right panel:
  Build/$K$ + \onlineq{} amortization over \(K\) queries. Rings mark the first
  checkpoints that solve all saved queries (black) and later checkpoints with
  at least 1\% path-ratio improvement (gold). PRM and BIT* curves are offline
  checkpoint envelopes.}
  \label{fig:tro_random_tradeoff}
\end{table*}

\begin{table*}[!t]
\centering
\TableFontRestore
\input{generated/tab_tro_endpoint_envelope.tex}
\vspace{0.45ex}
\input{generated/tab_tro_link_envelope.tex}
\end{table*}

\subsection{Shelf+IIWA Registered Profile Study}
\label{sec:exp_shelf_iiwa}

Fig.~\ref{fig:tro_shelf_scene} shows the Shelf+IIWA scene and fixed
five-anchor query cycle AS--TS--CS--LB--RB--AS; the anchor AS is the
approach-side anchor, TS and CS are shelf-side target anchors, and LB and RB are the
left and right bin anchors. Table~\ref{tab:tro-shelf-ablation} and
Fig.~\ref{fig:tro_shelf_tradeoff} report the b100 registered profile over seeds
0--7. This fixed cycle is used for mechanism ablations and profile selection;
the saved random catalog below tests broader start-goal variation. The table
reports build time, \onlineq{}, five-query amortized time
\(\mathrm{Build}/5+\onlineq{}\), and path ratio. Critical-sample rows use
the same planner and final query validation; their endpoint approximation is
treated as a non-certifying filter rather than as a conservative endpoint
certificate.

The registered \rbf{} baseline builds the reusable scene structure in
0.166 [0.163, 0.170]~s, has 0.130 [0.104, 0.150]~s \onlineq{}, and reaches
0.164 [0.137, 0.183]~s five-query amortized time under the d23 SupportHull
setting. Live HIFK-5 increases build time to 7.006 [6.947, 7.068]~s because it
includes live endpoint and envelope materialization. The paired rows make
the cache boundary explicit: warm rows measure replay, whereas the live row
measures materialization.
In the OBB-corridor profile, all 40 Shelf paths have zero residual
ordinary-segment fraction after corridor conversion. Link AABB and
SupportHull-without-broadphase isolate downstream representation costs: Link
AABB keeps a similar path-ratio level with slightly lower online cost in this
row, while skipping AABB broadphase approximately doubles amortized cost.

As a validation-boundary diagnostic, the registered pipeline returned 40/40
audited paths. An ablated variant with query bridge, repair, shortcutting, and
final simplification disabled returned 0/40; this result identifies which
auxiliary stages convert the reusable forest into the reported paths. The
reported shelf rows are final-validated results, not conservative-corridor
certificates.

\subsection{Shelf Cross-Algorithm Context}
\label{sec:exp_shelf_cross_algorithm}

The cross-algorithm shelf study uses the same five queries, 0.01-rad audit
segment step, and 0.010~s final-simplification budget for \rbf{}, IRIS-NP/GCS,
PRM, RRT-Connect, and BIT*. \rbf{} uses the b100 profile and per-query
\onlineq{} measurements; PRM and IRIS-NP/GCS report reusable builds;
RRT-Connect and BIT* solve each query independently. BIT* is plotted from
recorded traces, so its checkpoint row is an offline quality--time reference
rather than an online stopping rule. With these timing definitions, \rbf{}'s
warm-cache reusable build is 0.166~s; d23 prewarm is outside that row, and
snapshot load and resident memory are not included. PRM reports a 20.040~s
roadmap build and IRIS-NP/GCS reports a 355.425~s reusable build, while
RRT-Connect and BIT* retain lower latency on some individual queries.

\subsection{Random Multi-Robot Scenes}
\label{sec:exp_generalization}

The random-scene study uses saved prefix catalogs with ten queries per scene.
The catalogs are generated before planner execution and reused by every method.
The study covers KUKA
LBR iiwa, Universal Robots UR5, and Franka Emika Panda over medium and hard
difficulties and ten seeds, giving 600 saved query evaluations per planner
method. Hard scenes extend medium scenes, and trivial direct-line cases are
filtered with the same fixed-audit reference probes.

Table~\ref{tab:tro-random-summary} and Fig.~\ref{fig:tro_random_tradeoff}
summarize trade-off reference points. \rbf{} builds one query-agnostic
forest per scene and reuses it for ten queries; random rows use LECT caches and
anchors tied to each robot rather than Shelf-specific assets. For \rbf{}, each
displayed row is the registered OBB-corridor profile and solves all saved
queries; repair witnesses are accepted only after conversion into conservative \cspace{} OBB
corridor regions,
giving zero residual ordinary-segment fraction. PRM is built once per scene,
whereas RRT-Connect and BIT* solve each query independently. The archived
IRIS-NP/GCS saved-catalog run is retained as a feasibility diagnostic rather
than a table row because it did not produce fixed-audit rows comparable to the
other methods. Its saved-query audit outcomes were 0/100 successful hard-scene
queries and 0/100 (IIWA), 30/100 (UR5), and 10/100 (Panda) successful
medium-scene queries. The random-scene comparison therefore focuses on online
latency under shared saved catalogs:
single-query planners retain lower latency on individual queries, while \rbf{}
exposes the amortization available when the displayed forest is reused. At
those operating points, \rbf{} has lower \onlineq{} than the BIT* checkpoint
rows in four of six robot--difficulty panels and lower path ratio in the two
IIWA panels; BIT* has lower \onlineq{} and path ratio for UR5-Medium and
Panda-Hard.

\subsection{Mechanism Studies}
\label{sec:exp_mechanisms}

Tables~\ref{tab:tro-endpoint-envelope} and~\ref{tab:tro-link-envelope} isolate
endpoint sources and envelope narrow phases. \Cref{tab:tro-endpoint-envelope}
reports endpoint volume as a ratio to the Analytical row at the same width and
reports sampled-union signed gap in millimetres; negative gaps are
finite-sampling diagnostics. Only IFK and HIFK rows are certificate-backed;
Analytical is a reference computation for diagnostics rather than safety
evidence. \Cref{tab:tro-link-envelope} motivates the AABB broadphase:
SupportHull/GJK is compact, but wide boxes incur high collision-test time when
every obstacle reaches the GJK narrow phase.

\subsection{Dynamic Updates}
\label{sec:exp_dynamic_updates}

Table~\ref{tab:tro-dynamic-update} isolates scene-cache maintenance only: no
query bridge, connector, simplification, audit, or query timing is included.
\input{generated/tab_tro_dynamic_update.tex}
A diagnostic that attempted audited queries directly on maintenance-only forests
returned 0/8 paths for both updated and fresh forests. The table therefore
measures the local maintenance primitive, not a complete replanning pipeline.

\FloatBarrier
\section{Conclusion}\label{sec:conclusion}

\rbf{} is a link-envelope-guided box-forest planner for
multi-query manipulation. It uses axis-aligned boxes as the reusable region type:
endpoint-to-link swept-capsule bounds validate \cspace{}
boxes, accepted boxes form a scene-level forest and typed corridor graph, and
LECT separates reusable kinematics-keyed evidence from scene labels.
The certificate applies to conservative box and OBB-corridor unions; repaired
paths, shortened paths, and post-processed paths require independent query
validation.

Under saved catalogs, audit-separated timing, and explicit cache accounting,
warm d23 Shelf replay yields a 0.166~s reusable build and 0.130~s \onlineq{},
whereas live HIFK-5 evidence materialization increases build time to 7.006~s. In
saved random scenes, \rbf{} has lower \onlineq{} than the BIT*
checkpoint rows in four of six robot--difficulty panels at the displayed
operating points; the dynamic-update rows measure only scene-cache maintenance.
Within this scope, \rbf{} is a low-overhead method for constructing reusable
regions for repeated queries.
Remaining limitations include conservative corridors without repair, integrated
update-and-query pipelines, LECT snapshot load and memory break-even analysis,
and validation for self-collision, payloads, grippers, and task-specific
regions.

\begingroup
\let\origthebibliography\thebibliography
\let\endorigthebibliography\endthebibliography
\renewenvironment{thebibliography}[1]{%
  \origthebibliography{#1}%
  \scriptsize
  \linespread{0.88}\selectfont
  \setlength{\itemsep}{-0.55ex}%
  \setlength{\parsep}{0pt}%
}{\endorigthebibliography}
\IEEEtriggeratref{26}
\bibliographystyle{IEEEtran}
\bibliography{references}
\endgroup

\end{document}
